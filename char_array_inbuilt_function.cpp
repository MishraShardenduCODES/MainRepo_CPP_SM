#include <cstring>
#include <iostream>
using namespace std;

int main() {
    char str[] = "Hello, World!";
    size_t length = strlen(str);
    cout << "Length of str: " << length << endl;
    return 0;
}
